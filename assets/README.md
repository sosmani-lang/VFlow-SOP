# VFlow Recorder app icons

Place a `icon.ico` (256x256 multi-res) and `icon.png` (512x512) here before building.
For the first release these can be placeholders — electron-builder will fall back to its default if missing,
but Windows will look ugly. Recommended: drop a real branded icon before tagging `desktop-v*`.
