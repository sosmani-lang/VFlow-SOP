{
  "name": "vflow-desktop-recorder",
  "version": "0.2.1",
  "description": "VFlow Desktop SOP Recorder — captures clicks, screenshots and (opt-in) keystrokes on Windows.",
  "main": "src/main.cjs",
  "type": "commonjs",
  "private": true,
  "scripts": {
    "start": "electron .",
    "build": "electron-builder --win nsis --x64 --publish never",
    "build:portable": "electron-builder --win portable --x64 --publish never"
  },
  "author": "VFlow Apps",
  "license": "UNLICENSED",
  "dependencies": {
    "active-win": "^8.2.1",
    "electron-store": "^8.2.0",
    "uiohook-napi": "^1.5.4"
  },
  "devDependencies": {
    "electron": "^31.0.0",
    "electron-builder": "^24.13.3"
  }
}
